mutation SignIn($credentials: CredentialsSignin!) {
  signin(credentials: $credentials) {
    userErrors {
      message
    }
    diatonicToken
    user {
      admin
      email
      firstName
      lastName
      privateTeacher
      hasSignedIn
    }
  }
}
