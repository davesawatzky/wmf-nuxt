query CommunityInfo($registrationId: Int!) {
  registration(id: $registrationId) {
    community {
      id
      name
      address
      city
      province
      postalCode
      phone
      email
    }
  }
}
