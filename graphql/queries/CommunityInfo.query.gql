query CommunityInfo($registrationId: Int!) {
  registration(id: $registrationId) {
    community {
      id
      name
      groupSize
      chaperones
      wheelchairs
      earliestTime
      latestTime
      unavailable
      conflictPerformers
    }
  }
}
