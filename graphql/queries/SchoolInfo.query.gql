query SchoolInfo($registrationId: Int!) {
  registration(id: $registrationId) {
    school {
      id
      name
      division
      address
      city
      province
      postalCode
      phone
    }
  }
}
